attribute vec4 aPosition;
attribute vec2 aTexCoord;

uniform vec2 uPosition;

varying vec2 vTexCoord;

void main()								
{
	vec4 pos = aPosition;					
	pos.xy += uPosition;
	gl_Position = pos;

	vTexCoord = aTexCoord;
}